# K-mean-clustering
K-Means Clustering is an unsupervised machine learning algorithm used to group similar data points into clusters based on their features. It works by dividing the dataset into K predefined clusters, where each data point belongs to the cluster with the nearest mean (centroid).
